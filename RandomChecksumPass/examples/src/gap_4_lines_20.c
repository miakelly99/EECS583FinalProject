#include <stdio.h>

int main()
{
    //TODO: 
    // make one that has a much longer gap - like 100 lines?
    // make files that have increasing 
    int x = 5;
    int y = 10; // y is assigned here
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    y = y * x; // y is assigned here
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    y = y * x; // y is assigned here
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    y = y * x; // y is assigned here
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    x = x + 1; 
    x = x + 2; 
    x = x + 3; 
    x = x + 4; 
    x = x + 5; 
    x = x + 6; 
    x = x + 7; 
    x = x + 8; 
    x = x + 9; 
    x = x + 10;
    y = y * x; // y is assigned here
    printf("%d\n%d\n", x, y);
}